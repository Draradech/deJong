{
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@webgpu/types": "^0.1.52",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "http-server": "^14.1.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1"
  },
  "scripts": {
    "build": "tsc  &&  mkdir -p dist/  &&  cp static/* dist/",
    "serve": "npm run build && http-server dist",
    "deploy": "cd dist && git add * && git commit -a -m deploy && git push",
    "info": "!! deploy expects dist to be a git worktree pointing to the deployment branch"
  }
}
